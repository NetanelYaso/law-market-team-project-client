import React    from "react";
import template from "./PageNotFound.test.js";

class PageNotFound extends React.Component {
  render() {
    return template.call(this);
  }
}

export default PageNotFound;
